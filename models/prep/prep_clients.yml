version: 2

models:
  - name: prep_clients
    description: "Preparation model for client/customer data"
    columns:
      - name: client_id
        description: "Unique identifier for the client"
        data_tests:
          - not_null
          - unique
      - name: client_type
        description: "Type of client "
        tests:
          - not_null
      - name: first_name
        description: "First name of the client"
      - name: last_name
        description: "Last name of the client"
      - name: company_name
        description: "Name of the company if the client is a corporate entity"
      - name: date_of_birth
        description: "Date of birth of the client (if individual)"
      - name: nationality
        description: "Nationality of the client"
      - name: email
        description: "Email address of the client"
      - name: phone
        description: "Phone number of the client"
      - name: address
        description: "Street address of the client"
      - name: city_id 
        description: "Foreign key to the city where the client is located"
        data_tests:
          - relationships:
              arguments:
                to: ref('prep_cities')
                field: city_id
      - name: is_verified
        description: "Indicates whether the client's identity has been verified (true/false)"
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: [ 1 ]
      - name: registered_at
        description: "Timestamp when the client registered in the system"
        data_tests:
          - not_null

